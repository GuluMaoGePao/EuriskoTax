feat(ui): 阶段19-6b 我的页权益进度条 + 资产概览重做 + 功能按场景分组（v1.88.0）

权益进度条 #profile-benefits-card：顶栏徽标与横幅都只说档位，不说这一档给了什么、用了多少，
专业版因此永远只是按钮上的一个词。三格阶梯（基础版 → 体验版 → 专业版）点亮到当前档，下面两行
是真数：已用 N/M 套方案、云同步开没开（含上次同步时间）。

三条克制：进度条只表达档位在阶梯上的位置，不表达"距离专业版还差百分之几" —— 基础版到专业版之间
没有分母，任何百分比都是编出来的（真有分母的是方案用量，那个以文字写在下面一行）；取不到的整行
不出现，不补 0 —— "已存 0 套" 会被读成"你一套都没存"，而真相可能只是库没加载；升级入口未新增，
卡里那颗按钮就是横幅上那颗 #profile-nav-upgrade 搬了下来（id 与数量都不变，阶段14 入口收敛约束）。

资产概览四格全换：原「计算次数 / 档案数量 / 历史记录 / 本月提醒」里，计算次数与历史记录永远同一个数
（都读 taxCalculationHistory.length）、档案数量只有 0/1 两档、本月提醒只由当前月份决定 —— 两格同义
反复、一格是开关、一格是日历，这不叫资产概览。现为测算次数（本月 N 次）/ 已存方案 / 覆盖税种
（type|toolId 去重）/ 上次测算（N 天前 · 上次算出 ¥X），取不到一律「—」；最近一次按时间取最大而不是
取数组第一个（云同步合并后顺序不保证）；「本月提醒」改挂税务日历卡的 badge。

功能卡按场景分三组：我的数据 / 我的税务 / 服务与支持（代替平铺发射台）；badge 渲染时算一次即可，
不为此把整张卡改成每次进页面重渲染。

98 套件 1872 例全绿（新增 17 条：profile-benefits 16 + profile-page 卡片分组 1）、对比度审计 main
scope 0 处不达标、verify:local 259/259、preflight 通过、profile 两张截图基线重拍入仓。
