# EuriskoTax 反向代理：80 反代应用；443 备案后签发证书再启用（见文件末尾注释块）
server {
    listen 80;
    server_name _;

    # certbot webroot 校验路径（签发与续期都用它，必须放在反代之前）
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    client_max_body_size 10m;

    # gzip：HTML/JSON/JS/CSS 压缩，静态页体积小一大截
    gzip on;
    gzip_types text/html text/css application/javascript application/json image/svg+xml;
    gzip_min_length 1024;

    # 全部交给应用（应用自己 serve 静态页 + API，与 Zeabur 行为一致）
    location / {
        proxy_pass http://app:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 30s;
    }

    # 基础安全头
    add_header X-Content-Type-Options nosniff always;
    add_header X-Frame-Options SAMEORIGIN always;
    add_header Referrer-Policy strict-origin-when-cross-origin always;
}

# =============================================================================
# 备案通过 + 证书签发后：
#   1. 把 server_name 换成 euriskotax.com www.euriskotax.com
#   2. 取消下面整块注释，路径与 compose 里挂载的 ./certbot/conf 对应
#   3. nginx 容器里校验并重载：
#      docker compose -f deploy/lighthouse/docker-compose.yml exec nginx \
#        nginx -t && docker compose ... exec nginx nginx -s reload
#   4. 80 端口块里加一行永久跳转（保留 ACME 路径在前）：
#      location / { return 301 https://$host$request_uri; }
#      （原 proxy_pass 那段挪进 https 块即可）
#
# server {
#     listen 443 ssl;
#     http2 on;
#     server_name euriskotax.com www.euriskotax.com;
#
#     ssl_certificate     /etc/letsencrypt/live/euriskotax.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/euriskotax.com/privkey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#
#     client_max_body_size 10m;
#     gzip on;
#     gzip_types text/html text/css application/javascript application/json image/svg+xml;
#     gzip_min_length 1024;
#
#     location / {
#         proxy_pass http://app:3000;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto https;
#         proxy_read_timeout 30s;
#     }
#
#     add_header Strict-Transport-Security "max-age=31536000" always;
#     add_header X-Content-Type-Options nosniff always;
#     add_header X-Frame-Options SAMEORIGIN always;
#     add_header Referrer-Policy strict-origin-when-cross-origin always;
# }
# =============================================================================
