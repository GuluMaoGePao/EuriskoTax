{
  "build": {
    "command": "npm install --prefix server && npx prisma generate --schema=server/prisma/schema.prisma && npx prisma migrate deploy --schema=server/prisma/schema.prisma",
    "publish": "server"
  },
  "deploy": {
    "command": "node src/app.js",
    "port": 3000
  }
}
